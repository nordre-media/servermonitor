package party.rezruel.servermonitor.commands.constants

import party.rezruel.servermonitor.commands.*

val allCommands = mapOf(
    "log" to LogCommand,
    "config" to ConfigCommand,
    "schedulerestart" to ScheduleRestartCommand
)